import { GenerationBestTimePipe } from './generation-best-time.pipe';

describe('GenerationBestTimePipe', () => {
  it('create an instance', () => {
    const pipe = new GenerationBestTimePipe();
    expect(pipe).toBeTruthy();
  });
});
