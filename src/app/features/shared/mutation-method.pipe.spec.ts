import { MutationMethodPipe } from './mutation-method.pipe';

describe('MutationMethodPipe', () => {
  it('create an instance', () => {
    const pipe = new MutationMethodPipe();
    expect(pipe).toBeTruthy();
  });
});
